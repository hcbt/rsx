# Hardware documentation is the source

Primary sources are hardware documentation, CPU manuals, patents, and public tests whose license allows use. Another emulator's source is last resort: a failing test plus docs that do not explain the behaviour. What we learn is written down; their code is not pasted.
