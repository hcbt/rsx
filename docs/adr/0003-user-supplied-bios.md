# User-supplied BIOS, never in git

The SCPH-1001 BIOS is Sony's copyrighted firmware. This public repository never contains it. The user supplies `SCPH1001.BIN`; we load it from a documented local path and refuse to emulate if it is missing.
