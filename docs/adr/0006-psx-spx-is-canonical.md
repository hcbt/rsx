# PSX-SPX is the hardware spec

nocash PSX-SPX (`psx-spx.consoledev.net` and problemkaputt.de) is the canonical hardware document. MIPS R3000A manuals own the CPU. Sony and patent docs are used only when they state something SPX does not.
